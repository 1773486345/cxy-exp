# Direction B Result Index

This index freezes the meaning of the retained Direction B artifacts. It does
not move, delete, retrain, or reinterpret any result directory. B1 is the
retained controlled-mechanism baseline; every B2/B3 entry below is a closed
transfer hypothesis, not unfinished work.

| Phase | Artifact | Status | What the result establishes | Do not do |
| --- | --- | --- | --- | --- |
| B0 | Historical global-tail diagnostic; its tracked seed outputs are not present in the current worktree | Closed diagnostic | The paired dual-evidence construction was viable, but a global tail was unstable under heteroscedastic normal states | Restore or rerun it as an active baseline |
| B1 | `result/multi_evidence/b1_ecrc_seed3101_gpu/` through `seed3105_gpu/`; `b1_ecrc_summary_3101_3105/` | Passed, five frozen GPU seeds | Isolated temporal/cross repair plus input-restricted reliability calibration passes the declared two-regime synthetic mechanism gates | Claim transfer, real-data performance, or a fused deployment score |
| B2a-v1 | `result/multi_evidence/b2a_drift_seed4101_gpu/` | Closed, failed smoke | The first held-out continuous-drift donor contract did not guarantee terminal incompatibility and the all-target protocol failed | Run `4102..4105` or use it for transfer inference |
| B2a-GC | `result/multi_evidence/b2a_gc_seed4201_gpu/` | Closed, failed smoke | Certified terminal dependency-break orderings passed, while coherent normal controls and FPR stability failed | Retune B1 calibration or run confirmation seeds |
| B2c | `result/multi_evidence/b2c_fw_ecrc_seed4301_gpu/` | Closed, failed smoke | Family-wise, target/component/stratum cutoffs did not remove the continuous-drift normal-control failures | Run `4302..4305` or reuse its thresholds outside this protocol |
| B3a control | `result/multi_evidence/b3a_baseline_seed4401_gpu/` | Retained control | Same-seed B2a-GC control for the B3 checkpoint-isolation comparison | Treat it as a new B2 confirmation run |
| B3a | `result/multi_evidence/b3a_frozen_temporal_seed4401_gpu/` | Closed, valid failed comparison | Relation-history-conditioned cross repair preserved the certified dependency-break signal but did not resolve normal-control/FPR failures | Retune B3 or run another B3 seed |

The detailed protocol and gate rationale are retained in:

```text
B1_EXPERIMENT_PLAN.md
closed/B2A_EXPERIMENT_PLAN.md
closed/B2A_GC_EXPERIMENT_PLAN.md
closed/B2C_EXPERIMENT_PLAN.md
closed/B3_EXPERIMENT_PLAN.md
```

Direction A2 is independent of these result artifacts. It may report B1 as an
external baseline only through an A2-specific frozen comparison protocol; it
must not mutate `MultiEvidenceRepair`, select an A2 model from B scores, or
reuse a B threshold as an A2 threshold.
